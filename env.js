/*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ 
   /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ 
   /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ 
   /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ /*  const overlay = document.getElementById("login-overlay");
  const userInput = document.getElementById("login-user");
  const passInput = document.getElementById("login-pass");
  const msg = document.getElementById("login-msg");
  const btn = document.getElementById("login-btn");

  btn.addEventListener("click", () => {
    const u = userInput.value.trim().toLowerCase();
    const p = passInput.value;

    if (!u || !p) {
      msg.textContent = "Preencha todos os campos!";
      return;
    }

    if (users[u] && users[u] === p) {
      localStorage.setItem("loggedUser", u);
      window.location.href = "main.html";
    } else {
      msg.textContent = "Usuário ou senha incorretos!";
    }
  });

  // Se já estiver logado, pula direto
  const logged = localStorage.getItem("loggedUser");
  if (logged) {
    window.location.href = "main.html";
  }
    */ 
(function() {
  console.log('=== SISTEMA FROGS INICIADO ===');
  
  // SISTEMA DE AUTENTICAÇÃO
  const usersAuth = {
    "SbrFrog": "admin",
    "CoSbrFroggy": "coadmin", 
    "CooSbrFroga": "user1",
    "Fralight": "user2",
    "Froglex": "user3",
    "Frogatelli": "user4",
    "Frogmotor": "user5",
    "Froguel": "user6",
    "Frogjope": "user7",
    "Froramos": "user8",
    "Frogju": "user9",
    "Froguita": "user10",
    "Frage": "user11",
    "Frogster": "user12",
    "Franela": "user12+1",
    "Frogalves": "user14",
    "Frolazans": "user15",
    "Frogson": "user16",
    "Frobucki": "user17",
    "Froago": "user18",
    "Frohikaru": "user19",
    "Frogias": "user20",
    "Froguiel": "user21",
    "Froguelli": "user22",
    "Fraggy": "user23",
    "Fraguisa": "user24",
    "Frogacio": "user25",
    "Frobreira": "user26"
  };

  // SISTEMA DE DADOS INDIVIDUAIS
  const usersData = {
    "SbrFrog": { saldo: "infinito", emprego: "Líder", itens: [] },
    "CoSbrFroggy": { saldo: "65 Frog Leks", emprego: "Dono da Doceria do Neo Froggy e Co-Sbr", itens: [Bonecos: Boneco Gatinho da Margarete, Boneco da Margarete, Boneco do Marco, Boneco Robótico do Marco, Boneco da Marga, Boneco Rabanete, Boneco Milho do Obeso, Boneco Tomate da Marga, Boneco Cenoura do Marco e Boneco do Obeso, Boneco Cavaleiro da Margarete e Boneco Gosmento do Obeso.

Contas: Lilic, Passe, Comum, Enket e Perita.

Comidas: Suco de Detox da Marga, Brigadeiro da Marga, Lasanha do Obeso, Sushi da Margarete, Pizza do Marco, Torta de Abacaxi da Marga, Escondidinho do Obeso, Risoto da Margarete, Feijoada do Marco, Drink de Abacaxi da Marga, Cachaça do Obeso, Bolo de Morango da Margarete, Hambúrguer do Marco, Linguiça da Marca, Chocotone do Obeso, Macaron da Margarete, Pão Alho do Marco, Sonho do Obeso, Gelatina da Margarete, Refrigerante do Marco, Pão de Queijo do Obeso, Leitão do Obeso e Chopp do Marco.

Cartões: Cartão Frog, Cartão Bronze Frog, Cartão Quartzo Frog, Cartão Ametista Frog, Cartão Ferro Frog, Cartão Obsidiana Frog, Cartão Rubi Frog, Cartão Ouro Frog, Cartão Esmeralda Frog e Cartão Diamante Frog.] },
    "CooSbrFroga": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Fralight": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Froglex": { saldo: "37 Frog Leks", emprego: "Editor do Canva", itens: [Bonecos: Boneco do Obeso, Boneco Grudento do Obeso e Boneco Robótico do Marco
Comidas: Hambúrguer do Marco, Pão de Queijo do Obeso, Cachaça do Obeso, Drink de Abacaxi da Marga e o Bolo de Morango da Margarete
Cartões: Cartão Iniciante Frog, Cartão Frog e Cartão Ouro Frog] },
    "Frogatelli": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Frogmotor": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Froguel": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Frogjope": { saldo: "857 Frog Leks", emprego: "Editor do Canva", itens: [Boneco do robótico do Marco, Boneco da Margarete, Boneco Gosmento do Obeso, Boneco Gatinho da Margarete. Boneco Vitoria régia. De comida: Feijoada do Marco, Pao de queijo do Obeso, Bolo de morango, Drink de abacaxi, Cachaça do Obeso, Hambúrguer do Marco, Pizza do Marco, Sushi, Sonho, Refrigerante, Brigadeiro, Suco Detox, Gelatina. Doceria: Banana Split, Picolé, Pipoca, Bubble tea, Raspadinha, Doce de Leite, Goiabada, Croissant, Torta de maça, Donut, Churros, Macaron, Cupcake, Algodão Doce, Cookie, Bolo de Chocolate, Chocolate Branco, Bom bom. Contas: Conta Comum. Cartoes: Cartão Frog, Cartão Iniciante Frog, Frog Sunset, Universal, Vortex, Eclipse, Aurora.] },
    "Froramos": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Frogju": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Froguita": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Frage": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Frogster": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Franela": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Frogalves": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Frolazans": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Frogson": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Frobucki": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Froago": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Frohikaru": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Frogias": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Froguiel": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Froguelli": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Fraggy": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Fraguisa": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Frogacio": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] },
    "Frobreira": { saldo: "R$ 0,00", emprego: "Desempregado", itens: [] }
  };

  function initLoginSystem() {
    console.log('🔧 Inicializando sistema de login...');
    
    const userInput = document.getElementById("login-user");
    const passInput = document.getElementById("login-pass");
    const msg = document.getElementById("login-msg");
    const btn = document.getElementById("login-btn");

    // Verifica se elementos existem
    if (!userInput || !passInput || !msg || !btn) {
      console.error('❌ Elementos do login não encontrados!');
      setTimeout(initLoginSystem, 100);
      return;
    }

    console.log('✅ Elementos do login carregados!');

    function handleLogin() {
      const username = userInput.value.trim();
      const password = passInput.value;

      console.log('🔐 Tentando login para:', username);

      if (!username || !password) {
        msg.textContent = "Preencha todos os campos!";
        msg.style.color = "#ff6b6b";
        return;
      }

      // VERIFICA LOGIN
      if (usersAuth[username] && usersAuth[username] === password) {
        console.log('✅ Login bem-sucedido:', username);
        msg.textContent = "Login bem-sucedido! Redirecionando...";
        msg.style.color = "#51cf66";
        
        // SALVA DADOS DO USUÁRIO
        localStorage.setItem("loggedUser", username);
        
        const userData = usersData[username];
        if (userData) {
          localStorage.setItem("saldo_" + username, userData.saldo);
          localStorage.setItem("emprego_" + username, userData.emprego);
          localStorage.setItem("itens_" + username, JSON.stringify(userData.itens));
          console.log('💾 Dados salvos:', userData);
        }
        
        // REDIRECIONA
        setTimeout(() => {
          window.location.href = "main.html";
        }, 1500);
        
      } else {
        console.log('❌ Login falhou:', username);
        msg.textContent = "Usuário ou senha incorretos!";
        msg.style.color = "#ff6b6b";
      }
    }

    // EVENT LISTENERS
    btn.addEventListener("click", handleLogin);
    
    [userInput, passInput].forEach(input => {
      input.addEventListener('keypress', (e) => {
        if (e.key === 'Enter') handleLogin();
      });
    });

    console.log('🎯 Sistema de login pronto!');
  }

  // INICIALIZAÇÃO
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initLoginSystem);
  } else {
    initLoginSystem();
  }
})();
